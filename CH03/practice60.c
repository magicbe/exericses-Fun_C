/* practice60.c */
#include <stdio.h>
#include <stdlib.h>

int main()
{
	double x, y, z;

    printf("請輸入浮點數 x：");
    scanf("%d", &x);

    printf("請輸入浮點數 y：");
    scanf("%d", &y);

    printf("請輸入浮點數 z：");
    scanf("%d", &z);

    printf("x=%d, y=%d, z=%d\n", x, y, z);

    system("PAUSE");
    return 0;
}